<%= form_for(@login_page) do |f| %>
  <% if @login_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login_page.errors.count, "error") %> prohibited this login_page from being saved:</h2>

      <ul>
      <% @login_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_name %><br />
    <%= f.text_field :user_name %>
  </div>
  <div class="field">
    <%= f.label :user_pwd %><br />
    <%= f.text_field :user_pwd %>
  </div>
  <div class="actions">  
    <%= f.submit "Login" %>
  </div>
<% end %>                            